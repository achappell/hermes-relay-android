<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required on Android 11+ to see an installed speech recognizer. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!-- The Hermes relay is reached over the tailnet with wss://; no cleartext
         permission or network security configuration is required. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Tap-to-speak. Audio is transcribed on device and never uploaded: the
         relay refuses binary ingress and accepts local-STT text only. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Optional: scanning a Home pairing QR code. Link and code entry work without it. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HermesRelay">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- HOME-NW-17 pairing links, from a scanned QR code or a tapped link. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="hermes-home" android:host="pair" />
            </intent-filter>
        </activity>
    </application>
</manifest>
